// import { selectLiquidityPageDomain } from '../selectors';

describe('selectLiquidityPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
