/*
 * MarginTradingPage Messages
 *
 * This contains all the text for the MarginTradingPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MarginTradingPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MarginTradingPage container!',
  },
});
