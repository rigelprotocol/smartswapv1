// import { selectSmartSwappingPageDomain } from '../selectors';

describe('selectSmartSwappingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
